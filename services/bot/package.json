{
  "name": "@tag-media-bot/bot",
  "version": "1.0.0",
  "description": "Telegram bot",
  "author": "Vitaly Rudenko",
  "private": true,
  "license": "MIT",
  "main": "dist/app.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "dev": "ts-node-dev --env-file=.env --respawn --transpile-only src/app.ts",
    "lint": "eslint \"src/**/*.ts\" --fix"
  },
  "dependencies": {
    "@tag-media-bot/grpc": "*",
    "ioredis": "^5.3.2",
    "telegraf": "^4.15.3"
  }
}
